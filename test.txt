let obj = JSON.parse($response.body);

if (obj.data) {
    obj.data.badge_type = "gold";
    obj.data.is_gold = true;
    
    if (obj.data.data_v2) {
        obj.data.data_v2.premium_type = "gold";
        obj.data.data_v2.is_premium = true;
    }
    
    obj.data.premium_type = "gold";
    obj.data.is_premium = true;
}

$done({ body: JSON.stringify(obj) });
